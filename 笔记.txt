数据库：
    SQL：关系型数据库，mysql
    NoSQL：文档数据库 和 键值对数据库
    外键
    反规范化操作

    sqlalchemy:
    创建数据库:db.create_all()
    删除：   db.drop_all()
    增，删，改：数据有变动  先操作Python对象（创建对象或者修改对象的某一属性）,然后操作会话（add[增，改],delete[删]）,最后commit()
    查询:数据无变动：query

    更新表：1.删除旧的，再建一个表（数据丢失）
           2.数据库迁移

    10-24:
    用flask发邮件时需要注意不同的邮件服务商其服务器配置是不同的
    主要是注意以下几点（踩到的坑）：
    服务器域名（网址），使用什么证书进行验证：SSL还是其他的什么证书，端口号，用户名（邮箱地址），密码（有些邮箱为了安全，第三方登录时，需要使用授权码之类的作为密码使用）
    